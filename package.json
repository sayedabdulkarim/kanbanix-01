{
  "name": "kanbanix",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev:all",
    "dev:all": "concurrently -n \"CLIENT,MCP\" -c \"blue,green\" \"cd client && node server.js\" \"cd mcp-server && npm start\"",
    "dev:client": "cd client && npm run dev",
    "dev:mcp": "cd mcp-server && npm start",
    "install:all": "npm install && cd client && npm install && cd ../mcp-server && npm install",
    "postinstall": "cd client && npm install && cd ../mcp-server && npm install",
    "build": "cd client && npm run build",
    "start": "concurrently -n \"CLIENT,MCP\" -c \"blue,green\" \"cd client && npm start\" \"cd mcp-server && npm start\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}